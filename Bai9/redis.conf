# Cấu hình port
port 6379

# Yêu cầu mật khẩu để truy cập Redis
requirepass mysecretpassword

# Giới hạn bộ nhớ tối đa
maxmemory 256mb

# Chính sách xóa key khi đạt giới hạn bộ nhớ
maxmemory-policy allkeys-lru

# Kích hoạt persistence (lưu dữ liệu xuống đĩa)
appendonly yes