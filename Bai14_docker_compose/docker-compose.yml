services:
  app1:
    image: alpine:3.19
    command: sh -c "sleep infinity"
    container_name: app1
    networks:
      - private-network

  app2:
    image: alpine:3.19
    command: sh -c "sleep infinity"
    container_name: app2
    networks:
      - private-network

networks:
  private-network:
    driver: bridge 